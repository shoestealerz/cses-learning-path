# Your solutions

Save files as `<cses-id>-<short-name>.<extension>`, for example `1068-weird-algorithm.cpp`.

Keep the actual accepted solution and a short explanation in `../notes/<cses-id>.md`. Use the language you compete with. No prefilled solutions are provided.
